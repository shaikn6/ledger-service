version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - bodyclose
    - misspell
    - noctx
    - unconvert
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - bodyclose
          - noctx
